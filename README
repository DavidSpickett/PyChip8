PyChip8 is a Python interpreter of the CHIP-8 interpreted programming language. 
It requires at least Python 2.6 and PyGame along with roms and a "sound.wav" 
file for the blip sound.
See 'main.py --help' for usage. Example command line:

python main.py INVADERS --pixel-size=5 --fullscreen